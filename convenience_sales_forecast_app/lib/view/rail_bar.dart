import 'package:flutter/material.dart';

class RailBar extends StatelessWidget {
  const RailBar({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
