import 'package:flutter/material.dart';

class MapLocationPick extends StatelessWidget {
  const MapLocationPick({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
